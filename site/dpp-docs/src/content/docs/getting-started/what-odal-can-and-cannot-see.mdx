---
title: What Odal can and cannot see
description: "A precise statement of data access for a self-hosted node: the proof-bound architecture, stated as verifiable facts."
---

The proof-bound architecture means the import files are read once on the operator's infrastructure (validated, used to sign the passport, then discarded). The signed passport itself, carrying the full product data across its disclosure classes, is what is stored and served. This page states precisely what Odal can see, cannot see, and could see but does not.

It describes a self-hosted node, which is the only way Odal Node runs today. A node run for you is an option on the [roadmap](https://odal-node.io/roadmap), not something that exists; if it ever does, this page will describe it separately.

## What we can see

What anyone can see: the public part of the passports you publish, and your DID document, which is public by definition. We see these the same way any reader does, by looking them up through your resolver. We do not see who else looks them up; scan counts stay on your node.

## What we cannot see

- **Your node**: its servers, its database and its configuration. We are not part of the deployment and have no access to it.
- **Your signing key**: generated and held on your infrastructure, encrypted at rest with an Argon2id-derived AES-256-GCM key, and never transmitted.
- **The restricted parts of your passports**: they are released only to readers presenting a credential your node accepts.
- **Your import files, raw production data and supply-chain detail** beyond what you publish in a passport.

## What we could see but do not

Nobody, including the operator, keeps the contents of an import file. The software reads it once, validates the data, signs the passport and discards the file. There is no setting, configuration or code path that retains the uploaded file after signing; it is how the software works, not a choice made per customer.

Be precise about what that covers: the *file* is not kept, while records *derived* from it during the import are: the passport, and a job record carrying per-row validation findings.

## The mechanism

1. **Import**: product data arrives at your node (CSV, Excel or an ERP export) on infrastructure you control.
2. **Validate**: locally, against the product group's versioned schema and rules. Validation is a pure function: no network calls.
3. **Sign**: your Ed25519 key, held on your infrastructure, signs the validated passport as a JSON Web Signature bound to your `did:web` identity.
4. **Publish**: the signed passport becomes resolvable and the import file is discarded. Public fields are served to anyone; restricted fields only against a verified credential.
5. **Verify**: anyone verifies against your public DID document. Odal is not in the verification loop.

## Read next

- [Core Concepts](/core-concepts): the three governing principles, including this one.
- [Proof files and verification](/guides/verification): how anyone checks a passport without trusting us.
